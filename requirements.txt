# Full requirements — used for local dev and the training container.
# Serving and frontend containers will use their own trimmed-down
# requirements files (added in Phase 5/6) so those images stay small.

tensorflow==2.16.1
keras==3.3.3
numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.0
opencv-python-headless==4.10.0.84
pyyaml==6.0.1
kaggle==1.6.14
scikit-learn==1.5.0
pillow==10.3.0
